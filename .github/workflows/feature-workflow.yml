name: Feature Pipeline

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Test runner
        run: |
          echo "Hello from GitHub Actions"
          echo "Running on:"
          hostname
          whoami
